# نسخهٔ ۱.۳.۰ — سایت جدید (کلون egl.co.ae) + دکمهٔ Login

## چی عوض شد؟ (خیلی ساده)

وقتی کسی آدرس اصلی پروژه را باز کند (`/`)، دیگر آن صفحهٔ عمومی «Zenovix-Agent» را نمی‌بیند؛
حالا **یک کپی از سایت اصلی شرکت (egl.co.ae)** می‌بیند: همان متن‌ها، همان رنگ‌ها (سرمه‌ای و طلایی)،
همان لوگو و همان عکس‌ها. همهٔ عکس‌ها و لوگو داخل خود پروژه هستند (پوشهٔ `web/assets/`)،
پس سایت به egl.co.ae وابسته نیست.

سه چیز به آن کلون اضافه شده:

1. **دکمهٔ Login** (بالا سمت راست، در منوی موبایل و در پایین صفحه) → می‌رود به ورود مدیر
   (`/admin/ops/login.html`) و بعد از ورود، پنل مدیریت باز می‌شود. دقیقاً مثل صفحهٔ قبلی.
2. **فرم «Request a Quote / Tell Us What You Need»** واقعاً کار می‌کند: هر درخواست،
   داخل پنل مدیریت به شکل یک **تیکت جدید (کانال web)** + یک مشتری ثبت می‌شود و به فرستنده
   یک شمارهٔ پیگیری مثل `EGL-A8DA8929` نشان داده می‌شود.
3. **ویجت «Ask Zenovix»** پایین صفحه: بازدیدکننده همان‌جا از زنوویکس سؤال می‌پرسد و زنوویکس از
   دیتابیس دانش شرکت (همان FAQ و کاتالوگ) جواب می‌دهد. سؤال‌های مشکوک (مثل «دستورات قبلی
   را فراموش کن…») همان اول رد می‌شوند و اصلاً به مدل زبانی نمی‌رسند.

## محافظت‌ها

- فرم یک «تلهٔ ربات» مخفی دارد؛ اگر ربات آن را پر کند، چیزی ذخیره نمی‌شود.
- هر IP حداکثر ۶ فرم در ساعت و ۱۰ پیام چت در دقیقه می‌تواند بفرستد (بعدش جواب 429).
- اگر Redis در دسترس نباشد، این دو مسیر بسته می‌شوند (503) نه اینکه باز بمانند.
- جواب‌های زنوویکس از همان ۱۵ لایهٔ امنیتی ربات تلگرام رد می‌شوند و بدون ستاره/مارک‌داون هستند.

## چیزی که باید بدانید

- بخش «العربية» در بالای صفحه به نسخهٔ عربی سایت اصلی (egl.co.ae/ar) لینک می‌دهد.
- برای عوض کردن متن یا عکس‌های سایت: فایل `web/_build_site.py` را ویرایش کنید و
  دستور `python3 web/_build_site.py` را بزنید تا `web/index.html` دوباره ساخته شود.
  (اگر برنامه‌نویس نیستید، همین فایل را برای من بفرستید و بگویید چه چیزی عوض شود.)
- هیچ متغیر (Variable) جدیدی در Railway لازم نیست؛ همان لیست `railway-variables-EGL.env` کافی است.

## راه‌اندازی (مثل قبل، ۳ قدم)

1. محتوای این زیپ را جای فایل‌های گیت‌هاب بگذارید (همهٔ فایل‌ها).
2. Railway خودش دوباره Deploy می‌کند؛ مایگریشن‌ها خودکار اجرا می‌شوند.
3. آدرس سرویس را باز کنید: باید سایت EGL را ببینید؛ دکمهٔ Login بالا سمت راست است.
