# نسخه ۱.۲.۱ — رفع «بعد از start جواب نمی‌دهد»

این نسخه فقط اشکال‌های نسخه ۱.۲.۰ را که در لاگ Railway دیدیم درست می‌کند. هیچ قابلیت جدیدی اضافه نشده؛ همان نسخه نهایی است، به‌علاوه ۴ اصلاح.

## چه چیزی خراب بود؟

| # | نشانه در لاگ Railway | علت (به زبان ساده) | اصلاح |
|---|---|---|---|
| ۱ | `ingest failed` → `503` → `duplicate telegram message skipped` و مشتری جواب نمی‌گیرد | تلگرام به هر پیام یک شماره می‌دهد. برنامه این شماره را همراه پیام ذخیره می‌کند و دیتابیس اجازه شماره تکراری در یک چت را نمی‌دهد. وقتی شماره‌ها تکرار شوند (ربات/توکن جدید با دیتابیس قدیمی، یا پاک‌شدن حافظه Redis بعد از ری‌دیپلوی و ارسال دوباره توسط تلگرام) ذخیره‌کردن خطا می‌داد، کل دریافت پیام خراب می‌شد، تلگرام دوباره می‌فرستاد و دفعه دوم به‌عنوان «تکراری» دور ریخته می‌شد. | پیام تکراری‌شماره حالا با یک شماره یکتا ذخیره می‌شود و جواب داده می‌شود (`app/core/repository.py`). |
| ۲ | همان خطا ولی بدون توضیح؛ فقط `ingest failed` | متن واقعی خطا در خطی که Railway نشان می‌دهد چاپ نمی‌شد. | متن خطا داخل همان خط لاگ آمد: `ingest failed: UniqueViolationError: …` (`app/gateway/webhooks.py`). |
| ۳ | پیام‌های فارسی عادی بی‌جواب می‌ماندند (بدون خطا) | لایه امنیتی «نیم‌فاصله» فارسی (مثل «می‌خوام») را حمله‌ی یونیکد تشخیص می‌داد و پیام را رد می‌کرد. | نیم‌فاصله و اتصال ایموجی مجاز شدند؛ حمله‌های واقعی (کاراکترهای معکوس‌کننده متن) همچنان رد می‌شوند و ترفند «ig‌nore» با نیم‌فاصله هم گرفته می‌شود (`app/core/security_stack.py`). |
| ۴ | `GET /admin/api/approvals … 500` و `audit_write_failed` | صفحه Approvals داده‌های دیتابیس (شناسه، تاریخ، JSON) را با نوع اشتباه می‌خواند؛ جدول audit_log هم چهار ستونی که برنامه می‌نویسد را نداشت. | تبدیل نوع‌ها اصلاح شد (`app/admin_api/approvals.py`)؛ مهاجرت جدید `db/migrations/0016_audit_log_columns.sql` ستون‌ها را اضافه می‌کند (فقط اضافه می‌کند؛ چیزی پاک نمی‌شود). |

## چطور مطمئن شدیم؟

- همان سناریوی لاگ شما روی یک Postgres و Redis واقعی بازسازی شد: با کد ۱.۲.۰ همان `503` گرفتیم؛ با ۱.۲.۱ پیام ذخیره و جواب داده شد.
- پیام فارسی با نیم‌فاصله: در ۱.۲.۰ رد می‌شد، در ۱.۲.۱ جواب کامل گرفت.
- `/admin/api/approvals` حالا `200` برمی‌گرداند.
- ۱۴ تست جدید برای همین ۴ اشکال اضافه شد (`tests/unit/test_release_121_regressions.py`).
- همه گیت‌ها سبز: ruff، mypy، bandit، اسکن رمز، config-lint، **۱۲۶۵ تست واحد** و **۲۹۷ تست یکپارچه روی دیتابیس واقعی** (نسخه ۱.۲.۰ فقط تست‌های واحد را اجرا کرده بود).

## چطور دیپلوی کنم؟ (خیلی ساده)

1. محتویات این زیپ را جایگزین پروژه فعلی کنید و روی GitHub بفرستید (یا مستقیم در Railway دیپلوی کنید). مهاجرت شماره ۱۶ خودش موقع بالا آمدن اجرا می‌شود؛ نیازی به دستور دستی نیست.
2. **متغیرها هنوز قدیمی‌اند** (در لاگ، tenant هنوز `zenovix-digital` است). فایل `railway-variables-EGL.env` را باز کنید و مقدارها را در Railway → سرویس AGI → Variables بگذارید؛ مقدارهایی که با `PASTE_` شروع می‌شوند را با مقدار واقعی خودتان پر کنید.
3. بعد از دیپلوی، در تلگرام دو پیام پشت سر هم بفرستید (مثلاً `/start` و بعد «می‌خوام قیمت بدونم»). هر دو باید جواب بگیرند.
4. اگر باز مشکلی بود، حالا لاگ Railway علت دقیق را در همان خط `ingest failed: …` نشان می‌دهد؛ همان خط را بفرستید.

## یک سؤال برای شما

اگر اخیراً ربات تلگرام جدید ساختید یا توکن را عوض کردید، همین باعث تکرار شماره‌ها شده بود. با ۱.۲.۱ دیگر مشکلی ایجاد نمی‌کند، ولی اگر می‌خواهید تاریخچه گفتگوهای ربات قبلی با ربات جدید قاطی نشود، بگویید تا راهش را توضیح بدهم.
