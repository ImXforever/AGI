# نسخهٔ نهایی Zenovix 1.2.0 — چی عوض شد و چی‌کار کنی

این فایل همان چیزی است که خواستی: **همهٔ اصلاح‌ها یک‌جا**. نسخهٔ سبز CI (که هنوز دیپلوی نکرده بودی) + همهٔ باگ‌های لاگ + خواسته‌های جدید. یک بار دیپلوی می‌کنی، تمام.

---

## ۱) چی درست شد (به زبان ساده)

| مشکلی که دیدی | الان |
|---|---|
| «در حال نوشتن…» چند ثانیه بعد محو می‌شد و مشتری فکر می‌کرد بات مرده | تا لحظه‌ای که جواب فرستاده شود، هر ۴ ثانیه تازه می‌شود (حداکثر ۳ دقیقه) |
| بات نمی‌دانست کیست («من مدیر عملیات دیجیتال هستم» بدون اسم و شرکت) | **روح (Soul)** در دیتابیس: اسم، شرکت، نقش، مأموریت، شخصیت، لحن، زبان، معرفی، خط قرمزها. از پنل مدیریت قابل ویرایش است و در **همهٔ** پاسخ‌ها به مدل تزریق می‌شود |
| ستاره و `##` و `---` و جدول در پیام تلگرام | همهٔ Markdown قبل از ارسال به متن تمیز تبدیل می‌شود (پررنگ واقعی، بولت •). به مدل هم صریحاً گفته شده: بدون Markdown و JSON |
| ارور `fallback_templates_key_key` موقع بوت | Seed حالا با کلید طبیعی (`sku` / `key` / `id`) آپدیت می‌کند، دیگر خطا نمی‌دهد |
| هشدار checksum مایگریشن | فقط هشدار است؛ اگر روزی خواستی سخت‌گیر باشد: `MIGRATION_CHECKSUM_STRICT=true` |
| لاگ‌های عادی uvicorn به شکل ERROR | لاگ‌ها با سطح واقعی و به شکل JSON |
| ارور IMAP وقتی رمز ایمیل هنوز `PASTE_` است | ساکت می‌ماند تا رمز واقعی بگذاری |
| `/` باید صفحهٔ معرفی باشد | صفحهٔ **Landing** انگلیسی با دکمهٔ **Login** (همیشه دیده می‌شود) ← صفحهٔ ورود ← پنل مدیریت |
| پنل روی موبایل | منوی کشویی، چیدمان ستونی، دکمه‌های لمسی، آیکون‌ها؛ روی گوشی و کامپیوتر تست شد |
| تم | ۴ تم در بالای پنل: **Light · Dark · Gray · Neon** (انتخابت ذخیره می‌شود؛ `Alt+T` هم می‌چرخاند) |
| گرافیک | تصویر Landing، صفحهٔ ورود و صفحهٔ Soul به صورت JPEG داخلی (base64) — بدون فایل خارجی |
| ENG first + USD | پیش‌فرض ارز **USD**، اعداد غربی، سیاست پاسخ english-first، نام انگلیسی شرکت اصلی است، کاتالوگ USD |

آزمون‌ها: **۱۲۵۱ تست سبز**، lint / type / security / secret-scan / config-lint همه سبز → CI سبز می‌ماند.

---

## ۲) چطور دیپلوی کنی (یک بار)

1. فایل `AGI-final.zip` را باز کن، **کل محتوای پوشهٔ `AGI/`** را جایگزین ریپوی GitHub کن و push بزن (Railway خودش می‌سازد).
2. در Railway → سرویس AGI → **Variables** این‌ها را چک کن (بقیه از قبل هست):
   - `CURRENCY=USD`
   - `TENANT_NAME_EN=Elian Global Logistics Co. L.L.C`
   - `REPLY_LANGUAGE_POLICY=english-first`
   - `NUMERAL_STYLE=western`
   - (اختیاری) `MIGRATION_CHECKSUM_STRICT=false`
   فایل کامل و مرتب: `railway-variables-EGL.env`.
3. صبر کن سرویس بالا بیاید. در لاگ باید ببینی: `0015_agent_soul.sql` اجرا شد و `005_agent_soul.json` سید شد.
4. برو به `https://<دامنه>/` → دکمهٔ **Login** → با `ADMIN_USERNAME` و `ADMIN_BOOTSTRAP_PASSWORD` وارد شو.
5. در منوی سمت چپ روی **Soul** بزن، اسم/شرکت/معرفی را به دلخواه ویرایش کن و **Save soul**. از پیام بعدی اعمال می‌شود (بدون ری‌استارت).

---

## ۳) نکته‌ها

- روح پیش‌فرض برای EGL آماده است (Zenovix · Digital Operations Manager · Elian Global Logistics). اگر جدول به هر دلیلی نبود، بات با همین پیش‌فرض کار می‌کند و هیچ‌وقت جواب نمی‌افتد.
- تم‌ها فقط ظاهر پنل مدیریت را عوض می‌کنند، روی مشتری اثری ندارد.
- مسیر قدیمی `/admin/index.html` هنوز کار می‌کند و مستقیم به صفحهٔ ورود می‌رود.
- اگر خواستی دوباره اعداد فارسی/عربی بشود: `NUMERAL_STYLE=arabic-indic`.
